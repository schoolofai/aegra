# LangGraph Agent Server Configuration

# Database Configuration
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/langgraph_agent_server

# Authentication Configuration
# AUTH_TYPE can be "noop" (no authentication) or "custom" (custom implementation)
AUTH_TYPE=noop

# Server Configuration  
HOST=0.0.0.0
PORT=8000
DEBUG=true

# Example custom auth environment variables (customize as needed)
# FIREBASE_FUNCTIONS_BASE_URL=https://your-region-your-project.cloudfunctions.net
# JWT_SECRET_KEY=your-secret-key
# API_KEY_HEADER=x-api-key